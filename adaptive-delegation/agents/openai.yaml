interface:
  display_name: "Adaptive Delegation"
  short_description: "Codex-only Objective-Locked delegation"
  default_prompt: "Use $adaptive-delegation to construct one portable terminal-outcome Objective Lock, then route bounded work through Native fixed-role Luna-first escalation, Terra medium/high evidence-seeking intermediates after observable Luna quality failures, and Sol leaf roles only when evidence requires them."
policy:
  allow_implicit_invocation: true
