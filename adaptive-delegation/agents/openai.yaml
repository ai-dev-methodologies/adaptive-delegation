interface:
  display_name: "Adaptive Delegation"
  short_description: "Codex-only Objective-Locked delegation"
  default_prompt: "Use $adaptive-delegation to construct one portable terminal-outcome Objective Lock, then route bounded work Luna-first; use Terra medium/high for scoped or latency-sensitive recovery, Terra xhigh/max only for latency-insensitive quota-first goal work after observable Luna failure, and Sol leaf roles only when evidence requires them."
policy:
  allow_implicit_invocation: true
